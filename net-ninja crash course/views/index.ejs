<!DOCTYPE html>
<html lang="en">
<%- include('./partials/head.ejs') %>
<body>
    
    <!-- <% const name = 'mario' %> | when just doing code with no output structure like this-->
    <!--When you use the = it exludes special characters, instead the - sign allows for special characters; an addition note
     that you cannot have a comment on the same line as any of the ejs code syntax-->
    <%- include('./partials/nav.ejs') %>

    <div class="blogs content">
        <h2>All Blogs</h2>
        
        <% if (blogs.length > 0) { %>
            <% blogs.forEach(blog => { %>
                <a class="single" href="/blogs/<%= blog._id %>">
                    <h3 class="title"><%= blog.title %></h3>
                    <p class="snippet"><%= blog.snippet %></p>
                </a>
                <% }) %>
        <% } else { %>
            <p>There are no blogs to display...</p>
        <% } %>
    </div>

    <%- include('./partials/footer.ejs') %>
</body>
</html>