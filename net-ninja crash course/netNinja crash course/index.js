// const name = 'nick';

// console.log(name);

// const greet = (name) => {
//     console.log(`hello, ${name}`);
// }

// greet('mario');
// greet('yoshi');

//------------ v Global Object v ----------------

// console.log(global);

// setTimeout(() => {
//     console.log('in the timeout');
//     clearInterval(int);
// }, 3000);
//setTimeout() in this function prints out 'in the timeout' in the console once after 3 seconds,
// and after 3 seconds it also halts the setInterval function.

// const int = setInterval(() => {
// console.log('in the interval');
// }, 1000);
//setInterval() in this way however, outputs 'in the interval' every second. It only stops because
//setTimeout clear's the interval function.

// console.log(__dirname); //outputs the Directory Pathway
// console.log(__filename); //outputs the full file Pathway

//----------------- v Modules v ---------------
//created modules.js and people.js

